name: GCP Deploy

on:
  push:
    branches: [main, develop]
  workflow_dispatch:

concurrency:
  group: GCP-deploy
  cancel-in-progress: true

permissions:
  contents: read
  id-token: write
  
jobs:
  deploy:
    name: Deploy To GCP Server
    runs-on: ubuntu-latest
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set Up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set Up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Grant Gradle Wrapper Permission
        run: chmod +x ./gradlew

      - name: Build Boot Jar
        run: ./gradlew clean bootJar --no-daemon
        env:
          KAKAO_REST_API_KEY: ${{ secrets.KAKAO_REST_API_KEY }}

      - name: Upload Jar To Server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ vars.GCP_HOST }}
          username: ${{ vars.GCP_USERNAME }}
          key: ${{ secrets.GCP_SSH_KEY }}
          port: ${{ vars.GCP_PORT || 22 }}
          source: "build/libs/*.jar"
          target: ${{ vars.GCP_APP_DIR }}
          strip_components: 2

      - name: Restart Application
        uses: appleboy/ssh-action@v1.0.3
        env:
          APP_DIR: ${{ vars.GCP_APP_DIR }}
          SERVER_PORT: ${{ vars.GCP_SERVER_PORT || 3306 }}
          DB_URL: ${{ secrets.GCP_DB_URL }}
          DB_USERNAME: ${{ vars.GCP_DB_USERNAME }}
          JWT_SECRET: ${{ secrets.GCP_JWT_SECRET }}
          KAKAO_REST_API_KEY: ${{ secrets.KAKAO_REST_API_KEY }}
        with:
          host: ${{ vars.GCP_HOST }}
          username: ${{ vars.GCP_USERNAME }}
          key: ${{ secrets.GCP_SSH_KEY }}
          port: ${{ vars.GCP_PORT || 22 }}
          envs: APP_DIR,SERVER_PORT,DB_URL,DB_USERNAME,JWT_SECRET,JWT_ACCESS_TOKEN_VALIDITY_MS,JWT_REFRESH_TOKEN_VALIDITY_MS,KAKAO_REST_API_KEY,KAKAO_LOCAL_BASE_URL,AWS_S3_BUCKET,AWS_REGION
          script_stop: true
          script: |
            set -eu
            mkdir -p "$APP_DIR"
            cd "$APP_DIR"

            LATEST_JAR="$(find . -maxdepth 1 -type f -name '*.jar' ! -name 'app.jar' -printf '%T@ %p\n' | sort -nr | head -n 1 | cut -d' ' -f2-)"
            if [ -z "$LATEST_JAR" ]; then
              echo "No jar file was uploaded."
              exit 1
            fi

            mv "$LATEST_JAR" app.jar

            cat > .env <<EOF
            SERVER_PORT=${SERVER_PORT}
            DB_URL=${DB_URL}
            DB_USERNAME=${DB_USERNAME}
            JWT_SECRET=${JWT_SECRET}
            KAKAO_REST_API_KEY=${KAKAO_REST_API_KEY}
            EOF

            if pgrep -f "java -jar .*app.jar" >/dev/null 2>&1; then
              pkill -f "java -jar .*app.jar"
              sleep 5
            fi

            nohup java -jar app.jar > app.log 2>&1 &
            sleep 15

            if ! pgrep -f "java -jar .*app.jar" >/dev/null 2>&1; then
              echo "Application failed to start."
              tail -n 100 app.log || true
              exit 1
            fi
